# QuickDesk 信令服务器部署文档

## 快速部署（推荐）

Docker 一体化镜像，包含 PostgreSQL + Redis + 信令服务器。提供三种部署方式：

### 准备配置文件

```bash
git clone git@gitee.com:Barryda/QuickDesk.git
cd QuickDesk/SignalingServer

# 复制并修改配置
cp .env.example .env
vim .env
```

配置项说明：

**部署必填项（基础设施）：**

| 变量 | 默认值 | 说明 |
|------|--------|------|
| `SERVER_HOST` | 0.0.0.0 | 监听地址 |
| `SERVER_PORT` | 8000 | 服务端口 |
| `DB_HOST` | localhost | 数据库地址 |
| `DB_PORT` | 5432 | 数据库端口 |
| `DB_USER` | quickdesk | 数据库用户名 |
| `DB_PASSWORD` | quickdesk123 | 数据库密码 |
| `DB_NAME` | quickdesk_signaling | 数据库名 |
| `REDIS_HOST` | localhost | Redis 地址 |
| `REDIS_PORT` | 6379 | Redis 端口 |
| `REDIS_PASSWORD` | (空) | Redis 密码 |
| `ADMIN_USER` | admin | 初始管理员用户名（仅首次部署有效） |
| `ADMIN_PASSWORD` | admin | 初始管理员密码（仅首次部署有效） |

**可选项（部署后可在管理后台动态配置）：**

以下参数可以在 `.env` 中预设，也可以部署完成后在管理后台「系统设置」页面随时修改，无需重启服务：

| 变量 | 默认值 | 说明 |
|------|--------|------|
| `TURN_URLS` | (空) | TURN 服务器地址（逗号分隔） |
| `TURN_AUTH_SECRET` | (空) | TURN 共享密钥（匹配 coturn 的 static-auth-secret） |
| `TURN_CREDENTIAL_TTL` | 86400 | TURN 凭据有效期（秒） |
| `STUN_URLS` | (空) | STUN 服务器地址（逗号分隔） |
| `API_KEY` | (空) | 客户端认证 API Key（空=不启用） |
| `ALLOWED_ORIGINS` | (空) | WebClient 域名白名单（逗号分隔） |
| `ALIYUN_SMS_ACCESS_KEY_ID` | (空) | 阿里云短信 AccessKey ID |
| `ALIYUN_SMS_ACCESS_KEY_SECRET` | (空) | 阿里云短信 AccessKey Secret |
| `ALIYUN_SMS_SIGN_NAME` | (空) | 阿里云短信签名 |
| `ALIYUN_SMS_TEMPLATE_CODE` | (空) | 阿里云短信模板 Code |

> **注意：** `.env` 中的可选参数仅在首次部署时写入数据库。之后所有修改请通过管理后台进行，`.env` 中的值不再生效。
> 
> **短信功能：** 阿里云短信用于手机号验证码登录/注册。四个字段全部填写后短信功能自动启用，任一为空则自动禁用。

### 方式一：拉取预构建镜像部署（推荐）

无需本地编译，直接从 GitHub Container Registry 拉取预构建镜像：

```bash
chmod +x deploy-pull.sh

# 部署最新版
./deploy-pull.sh

# 指定版本
./deploy-pull.sh v1.0.0

# 自定义端口
./deploy-pull.sh --port 9000
```

### 方式二：本地编译部署

从源码构建 Docker 镜像。适用于需要定制代码或网络无法拉取镜像的场景：

```bash
chmod +x deploy-build.sh
./deploy-build.sh

# 自定义端口
./deploy-build.sh --port 9000
```

### 方式三：离线部署

适用于服务器无法访问外网的场景。先从 GitHub Actions Artifacts 或 Releases 下载离线镜像，然后加载部署：

```bash
# 1. 在有网络的机器上下载离线镜像（.tar.gz）
#    - GitHub Actions → SignalingServer Docker → Artifacts
#    - GitHub Releases（tagged 版本）

# 2. 传输到目标服务器后部署
chmod +x deploy-offline.sh
./deploy-offline.sh quickdesk-signaling-image.tar.gz
```

### 传统部署方式

原有的一键部署脚本仍然可用，支持 Nginx 反向代理和 SSL 配置：

```bash
chmod +x deploy.sh
./deploy.sh

# 带域名和 Nginx 反向代理
./deploy.sh --port 8000 --domain your-domain.com
```

### 部署后配置

部署完成后，请立即登录管理后台完成以下配置：

1. **修改管理员密码**：访问管理后台 → 管理员账户 → 编辑管理员，修改用户名和密码
2. **配置 ICE 服务器**：访问管理后台 → 系统设置 → ICE / TURN / STUN，添加你的 TURN/STUN 服务器
3. **配置安全选项**：
   - **API Key**：访问管理后台 → 系统设置 → API Key。设置后，只有携带此 Key 的客户端才能连接信令服务器，可防止未授权客户端接入。原生客户端（QuickDesk 桌面端）可在 **设置 → 网络 → API Key** 中运行时配置，无需重新编译
   - **Allowed Origins**（WebClient 域名白名单）：访问管理后台 → 系统设置 → Allowed Origins。**WebClient 单独部署在不同域名时必须配置**（例如 `https://web.quickdesk.cc`），有两个原因：
     1. CORS — 浏览器同源策略会阻止跨域请求
     2. `POST /v1/devices/:id/access-code:verify` 接口对浏览器调用方走 **Origin 白名单鉴权**（浏览器 fetch 无法附加 `X-API-Key`）。**漏配此项会导致 WebClient 的"连接"按钮静默 403**
     
     多个域名用逗号分隔，例如 `https://web.quickdesk.cc,https://app.quickdesk.cc`
4. **配置 WebClient URL**（可选）：访问管理后台 → 预设配置 → WebClient URL。填写 WebClient 的部署地址（如 `https://web.quickdesk.cc`），客户端将在界面中展示该链接，方便用户快速访问 WebClient
5. **配置短信服务**（可选）：访问管理后台 → 系统设置 → Aliyun SMS，填写阿里云短信的 AccessKey、签名和模板，启用手机号验证码登录功能

这些配置修改后**立即生效**，无需重启服务。

### Docker Compose 管理

使用 `deploy-pull.sh` 或 `deploy-offline.sh` 部署后，可使用标准 docker compose 命令管理：

```bash
# 查看状态
docker compose ps

# 查看日志
docker compose logs -f

# 停止服务
docker compose down

# 重启服务
docker compose restart
```

---

## 手动部署（分步安装）

以下是不使用一体化 Docker 的手动安装步骤。

### 系统要求

- CentOS 7/8/Stream 或 Rocky Linux 8+
- 2GB+ 内存
- 10GB+ 磁盘空间
- 公网 IP 和域名（可选）

## 1. 安装 Docker

```bash
# 安装 Docker
sudo yum install -y yum-utils
sudo yum-config-manager --add-repo https://download.docker.com/linux/centos/docker-ce.repo
sudo yum install -y docker-ce docker-ce-cli containerd.io
sudo systemctl start docker
sudo systemctl enable docker

# 验证安装
docker --version
```

## 2. 部署数据库（Docker）

```bash
# 创建数据目录
sudo mkdir -p /data/quickdesk/{postgres,redis}

# 启动 PostgreSQL
docker run -d --name quickdesk-postgres \
  --restart=always \
  -p 5432:5432 \
  -e POSTGRES_USER=quickdesk \
  -e POSTGRES_PASSWORD=quickdesk123 \
  -e POSTGRES_DB=quickdesk \
  -v /data/quickdesk/postgres:/var/lib/postgresql/data \
  postgres:15

# 启动 Redis
# 重要：必须开启 keyspace 通知，让 host 心跳 TTL 过期能转化为
# `device.online.changed` 实时事件。命令行加 --notify-keyspace-events Ex
# （大写 E + 小写 x），或在 redis.conf 里写 notify-keyspace-events Ex。
docker run -d --name quickdesk-redis \
  --restart=always \
  -p 6379:6379 \
  -v /data/quickdesk/redis:/data \
  redis:7 redis-server --appendonly yes --notify-keyspace-events Ex

# 验证运行状态
docker ps
```

> **为什么必须开这个 Redis 标志** —— v1 设计用两个信号（heartbeat + 信令 WS
> 连接）追踪设备在线状态，分别存为带 TTL 的 Redis key。当 host 进程崩溃时，
> 只有 TTL 过期事件能告诉服务端它已离线。如果没有 `notify-keyspace-events Ex`，
> 服务端永远收不到过期通知，`online` 会一直卡在 `true`，直到下一次有 client
> 尝试连接才被动发现。Redis 7 默认镜像**未启用**通知。
>
> 完整状态机原理见 `QuickDesk/docs/dev/信令服务器API重构方案.md` §2.4 / §2.17。

## 3. 安装 Go 环境

```bash
# 下载 Go 1.24
cd /tmp
wget https://go.dev/dl/go1.24.0.linux-amd64.tar.gz

# 解压安装
sudo rm -rf /usr/local/go
sudo tar -C /usr/local -xzf go1.24.0.linux-amd64.tar.gz

# 配置环境变量
echo 'export PATH=$PATH:/usr/local/go/bin' >> ~/.bashrc
echo 'export GOPROXY=https://goproxy.cn,direct' >> ~/.bashrc
source ~/.bashrc

# 验证安装
go version
```

## 4. 安装 Node.js（构建前端）

```bash
# 安装 Node.js 20 LTS
curl -fsSL https://rpm.nodesource.com/setup_20.x | sudo bash -
sudo yum install -y nodejs

# 验证安装
node --version
npm --version
```

## 5. 编译信令服务器

> **注意：** 数据库表结构由信令服务器启动时 GORM AutoMigrate 自动创建/更新，无需手动执行 SQL。
> 参考 SQL 见 `SignalingServer/migrations/001_init.sql`。

```bash
# 克隆或上传代码到服务器
cd /opt
git clone git@gitee.com:Barryda/QuickDesk.git
cd QuickDesk/SignalingServer

# 构建前端（管理后台 + 用户门户，Vue 3 + Element Plus）
cd web
npm install
npm run build
cd ..

# 下载 Go 依赖
go mod tidy

# 编译（前端构建产物会通过 go:embed 嵌入二进制）
CGO_ENABLED=0 GOOS=linux GOARCH=amd64 \
  go build -a -ldflags="-s -w -extldflags '-static'" \
  -o quickdesk_signaling ./cmd/signaling

# 创建运行目录并部署
sudo mkdir -p /opt/quickdesk-signaling
sudo cp quickdesk_signaling /opt/quickdesk-signaling/
```

## 6. 配置并运行服务

```bash
# 复制配置模板到运行目录
sudo cp .env /opt/quickdesk-signaling/.env

# 按需修改配置（数据库地址、密码、TURN 配置、API Key 等）
sudo vim /opt/quickdesk-signaling/.env

# 创建 systemd 服务
sudo cat > /etc/systemd/system/quickdesk-signaling.service << 'EOF'
[Unit]
Description=QuickDesk Signaling Server
After=network.target docker.service
Requires=docker.service

[Service]
Type=simple
User=root
WorkingDirectory=/opt/quickdesk-signaling
ExecStart=/opt/quickdesk-signaling/quickdesk_signaling
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
EOF

# 启动服务
sudo systemctl daemon-reload
sudo systemctl start quickdesk-signaling
sudo systemctl enable quickdesk-signaling

# 检查服务状态
sudo systemctl status quickdesk-signaling
```

服务启动后，Go 服务器默认读取工作目录下的 `.env` 文件。也可以通过 `--env` 参数指定任意路径的配置文件：

```bash
# 指定配置文件路径
/opt/quickdesk-signaling/quickdesk_signaling --env /etc/quickdesk/production.env
```

也可以修改 systemd 服务中的 `ExecStart`，添加 `--env` 参数。

后续修改配置只需编辑对应的 env 文件，然后重启服务：

```bash
sudo vim /opt/quickdesk-signaling/.env
sudo systemctl restart quickdesk-signaling
```

## 7. 配置防火墙

```bash
# 开放端口
sudo firewall-cmd --permanent --add-port=8000/tcp
sudo firewall-cmd --permanent --add-port=80/tcp
sudo firewall-cmd --permanent --add-port=443/tcp
sudo firewall-cmd --reload

# 或者关闭防火墙（不推荐生产环境）
# sudo systemctl stop firewalld
# sudo systemctl disable firewalld
```

## 8. 域名访问配置（Nginx 反向代理）

```bash
# 安装 Nginx
sudo yum install -y nginx
sudo systemctl start nginx
sudo systemctl enable nginx

# 配置反向代理
sudo cat > /etc/nginx/conf.d/quickdesk.conf << 'EOF'
# 关键：根据请求类型动态设置 Connection 头
# WebSocket 请求带 Upgrade 头时设为 upgrade，普通 HTTP 请求设为 close
map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}

upstream signaling {
    server 127.0.0.1:8000;
}

server {
    listen 80;
    server_name your-domain.com;  # 替换为你的域名

    client_max_body_size 100M;

    # v1 实时 WebSocket 端点（长连接）
    #   /v1/realtime/events  — 用户设备状态事件流（Qt + WebClient）
    #   /v1/realtime/signal  — WebRTC SDP/ICE 中继（host + client）
    # 服务端每 60s 发一次 Ping，设 proxy_read_timeout > Ping 间隔；
    # 300s 留足网络抖动的容错。
    location /v1/realtime/ {
        proxy_pass http://signaling;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_read_timeout 300s;
        proxy_send_timeout 300s;

        # 禁用缓冲，确保实时通信
        proxy_buffering off;
    }

    # HTTP API（/v1/*）以及 admin SPA 静态资源（/admin/、/）
    location / {
        proxy_pass http://signaling;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 30s;

        # CORS 预检 — WebClient 多部署在子域名上，请在后台
        # 「系统设置 → Allowed Origins」里加上 WebClient 域名；
        # 信令服务器会自动下发 Access-Control-* 响应头。
    }
}
EOF

# 测试配置
sudo nginx -t

# 重启 Nginx
sudo systemctl restart nginx
```

## 9. HTTPS 配置（可选）

```bash
# 安装 Certbot
sudo yum install -y epel-release
sudo yum install -y certbot python3-certbot-nginx

# 申请证书（自动配置 Nginx）
sudo certbot --nginx -d your-domain.com

# 自动续期
sudo certbot renew --dry-run

# Certbot 会自动添加定时任务，无需手动配置
```

配置完成后，Nginx 配置会自动更新为：

```nginx
server {
    listen 443 ssl http2;
    server_name your-domain.com;

    ssl_certificate /etc/letsencrypt/live/your-domain.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/your-domain.com/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

    # v1 实时 WebSocket 端点
    location /v1/realtime/ {
        proxy_pass http://127.0.0.1:8000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 300s;
        proxy_send_timeout 300s;
        proxy_buffering off;
    }

    # HTTP API
    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

server {
    listen 80;
    server_name your-domain.com;
    return 301 https://$server_name$request_uri;
}
```

## 10. 验证部署

```bash
# 检查服务状态
sudo systemctl status quickdesk-signaling
sudo systemctl status nginx

# 检查端口监听
sudo netstat -tlnp | grep -E '8000|80|443'

# 测试 API（本地）
curl http://localhost:8000/health

# 测试 API（域名）
curl http://your-domain.com/health
curl https://your-domain.com/health  # HTTPS
```

### `/health` 健康检查端点

`GET /health` 是规范的存活/就绪探针。设计上**公开**（不需鉴权），让 Kubernetes /
负载均衡的健康检查无需配置凭证即可访问。健康响应：

```json
{
  "status":  "ok",
  "version": "v2.10.0",
  "components": {
    "postgres": "ok",
    "redis":    "ok"
  }
}
```

行为说明：

- 所有 component 报 `ok` → HTTP `200`，可直接作为 readiness probe
- 任一 component 异常 → HTTP `503`，`status: "degraded"`，`components` 字段告知具体哪个组件故障（DB 不可达、Redis 缺失等）
- `version` 字段来自构建时的 ldflag 注入：`-X main.Version=$(git describe --tags --always)`；未设置时默认 `"dev"`

Kubernetes 探针推荐：

```yaml
livenessProbe:
  httpGet: { path: /health, port: 8000 }
  initialDelaySeconds: 5
  periodSeconds: 10
  failureThreshold: 6     # 容忍约 60s degraded 后重启容器

readinessProbe:
  httpGet: { path: /health, port: 8000 }
  periodSeconds: 5
  failureThreshold: 2     # 快速从 LB 摘除
```

通过 nginx 反代后，`/health` 在公网域名同样可访问，可作为外部冒烟测试：

```bash
# 期望 200 + JSON
curl -i https://your-domain.com/health
```

监控系统只需要状态码：

```bash
curl -fsS -o /dev/null -w '%{http_code}\n' https://your-domain.com/health
# → 200 健康 / 503 降级 / 连接错误 = 服务挂了
```

## 11. 日志查看

```bash
# 查看信令服务器日志
sudo journalctl -u quickdesk-signaling -f

# 查看 Nginx 日志
sudo tail -f /var/log/nginx/access.log
sudo tail -f /var/log/nginx/error.log

# 查看 Docker 容器日志
docker logs -f quickdesk-postgres
docker logs -f quickdesk-redis
```

## 12. 常用运维命令

```bash
# 重启服务
sudo systemctl restart quickdesk-signaling
sudo systemctl restart nginx

# 停止服务
sudo systemctl stop quickdesk-signaling

# 查看数据库连接
docker exec -it quickdesk-postgres psql -U quickdesk -d quickdesk

# 查询设备数量
docker exec -it quickdesk-postgres psql -U quickdesk -d quickdesk -c "SELECT COUNT(*) FROM devices;"

# 备份数据库
docker exec quickdesk-postgres pg_dump -U quickdesk quickdesk > /backup/quickdesk_$(date +%Y%m%d).sql

# 恢复数据库
cat backup.sql | docker exec -i quickdesk-postgres psql -U quickdesk -d quickdesk

# 查看预设配置（需先拿到 admin access_token）
curl http://localhost:8000/v1/admin/preset \
  -H "Authorization: Bearer $ADMIN_ACCESS_TOKEN"

# 更新预设配置（也可通过管理后台 /admin/ 页面操作）
curl -X PUT http://localhost:8000/v1/admin/preset \
  -H "Authorization: Bearer $ADMIN_ACCESS_TOKEN" \
  -H "Content-Type: application/json" \
  -d @test_preset.json
```

## 故障排查

### 服务无法启动

```bash
# 查看详细日志
sudo journalctl -u quickdesk-signaling -n 100 --no-pager

# 检查端口占用
sudo lsof -i:8000

# 检查数据库连接
docker exec -it quickdesk-postgres psql -U quickdesk -d quickdesk -c "SELECT 1;"
docker exec -it quickdesk-redis redis-cli ping
```

### Nginx 502 错误

```bash
# 检查信令服务是否运行
sudo systemctl status quickdesk-signaling

# 检查 SELinux 状态（可能阻止 Nginx 连接）
sudo setenforce 0  # 临时关闭
sudo sed -i 's/SELINUX=enforcing/SELINUX=disabled/g' /etc/selinux/config  # 永久关闭

# 或者配置 SELinux 允许 Nginx 网络连接
sudo setsebool -P httpd_can_network_connect 1
```

### 数据库连接失败

```bash
# 检查 Docker 容器状态
docker ps -a

# 重启数据库容器
docker restart quickdesk-postgres quickdesk-redis

# 检查数据库日志
docker logs quickdesk-postgres
```

## 安全建议

1. **修改默认密码**：修改 PostgreSQL 和 Redis 密码
2. **配置防火墙**：仅开放必要端口（80、443）
3. **启用 HTTPS**：生产环境必须使用 HTTPS
4. **定期备份**：设置定时任务备份数据库
5. **日志监控**：配置日志收集和告警
6. **限流配置**：在 Nginx 中配置请求限流
7. **客户端认证**：在管理后台「系统设置」中配置 API Key 限制原生客户端访问，配置 Allowed Origins 限制 WebClient 访问

```nginx
# Nginx 限流配置示例
http {
    limit_req_zone $binary_remote_addr zone=api_limit:10m rate=10r/s;
    
    server {
        location /api/ {
            limit_req zone=api_limit burst=20 nodelay;
        }
    }
}
```

## 生产环境配置建议

```bash
# 1. 修改数据库密码（强密码）
# 停止容器
docker stop quickdesk-postgres
docker rm quickdesk-postgres

# 使用新密码重新创建
docker run -d --name quickdesk-postgres \
  --restart=always \
  -p 5432:5432 \
  -e POSTGRES_USER=quickdesk \
  -e POSTGRES_PASSWORD='your-strong-password-here' \
  -e POSTGRES_DB=quickdesk \
  -v /data/quickdesk/postgres:/var/lib/postgresql/data \
  postgres:15

# 2. 同步更新 .env 文件中的 DB_PASSWORD
vim .env

# 3. 重新部署
./deploy.sh
```

## 访问地址

部署完成后，可通过以下地址访问：

- **HTTP**: `http://your-domain.com`
- **HTTPS**: `https://your-domain.com`
- **健康检查**: `https://your-domain.com/health`
- **WebSocket（实时事件流）**: `wss://your-domain.com/v1/realtime/events`（首帧 auth 鉴权，详见 user-api-docs §7.1）
- **WebSocket（信令中继）**: `wss://your-domain.com/v1/realtime/signal`（首帧 auth 鉴权，详见 user-api-docs §7.2）
- **API 入口**: `https://your-domain.com/v1/`（完整路由表见 `SignalingServer/docs/user-api-docs.md`）
- **管理后台**: `https://your-domain.com/admin/`（监控面板/设备管理/用户管理/管理员账户/系统设置）
- **WebClient**: 独立部署，通过 API 与信令服务器通信）
- **WebClient**: 独立部署，通过 API 与信令服务器通信

## 性能优化

```bash
# 1. 调整 Nginx worker 数量
# 编辑 /etc/nginx/nginx.conf
worker_processes auto;
worker_connections 4096;

# 2. 配置 PostgreSQL 连接池
# 在代码配置中设置适当的连接池大小

# 3. Redis 持久化配置
docker run -d --name quickdesk-redis \
  --restart=always \
  -p 6379:6379 \
  -v /data/quickdesk/redis:/data \
  redis:7 redis-server \
    --appendonly yes \
    --maxmemory 512mb \
    --maxmemory-policy allkeys-lru
```
